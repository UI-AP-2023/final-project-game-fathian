package com.example.demo.model;

public class Tower extends Building{
    public Tower(String urlImage, BuildingCategory buildingCategory, double health, double attackPower) {
        super(urlImage, buildingCategory, health, attackPower);
    }
}
